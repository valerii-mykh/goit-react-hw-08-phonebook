import { Typography } from '@mui/material';

const NotFound = () => {
  return (
    <div>
      <Typography component="p" variant="h4" textAlign="center" mt={24}>
        Page not found
      </Typography>
    </div>
  );
};
export default NotFound;
